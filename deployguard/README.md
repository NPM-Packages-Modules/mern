# deployguard

```bash
npx @mr-aftab-ahmad-khan/deployguard check .
```

Validates **`.env.example` vs `process.env`**, probes a **Dockerfile** when present, estimates **dependency weight**, and (if `mongodb` is installed + URI set) **pings MongoDB**.

## License

MIT
